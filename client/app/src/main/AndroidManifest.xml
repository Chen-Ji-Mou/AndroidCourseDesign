<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chenjimou.androidcoursedesign">
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/MaterialAppTheme">
        <activity
            android:name=".ui.activity.SpaceDetailActivity"
            android:theme="@style/ImmersiveTheme" />
        <activity
            android:name=".ui.activity.SharingImageSelectActivity"
            android:theme="@style/ToolBarAppTheme" />
        <activity
            android:name=".ui.activity.ShareEditActivity"
            android:theme="@style/ToolBarAppTheme" />
        <activity
            android:name=".ui.activity.RegisterActivity"
            android:theme="@style/ImmersiveTheme" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:launchMode="singleTop"
            android:theme="@style/ImmersiveTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
    
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.MainActivity"
            android:theme="@style/ToolBarAppTheme"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden" />
    </application>

</manifest>